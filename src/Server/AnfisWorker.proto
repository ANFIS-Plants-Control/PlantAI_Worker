syntax = "proto3";

option csharp_namespace = "Infrastructure.AiServices.Anfis";

service NetAnswer{
	rpc GetNetAnswer(SensorDatas) returns (ResponseValue);
}

message SensorDatas {
	float temperature = 1;
	float humidity = 2;
	float co2 = 3;
}

message ResponseValue{
	float ans = 1;
}

